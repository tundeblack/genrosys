import React from "react";
import { hot } from "react-hot-loader";

function EmployeeRecord(props)
{
    return (
        <section className="row">
            <div className="col-md-2"></div>
        </section>
    );
}

export default hot(module)(EmployeeRecord);